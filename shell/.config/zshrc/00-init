export EDITOR=nvim

DIRS=(
    "$HOME/.oh-my-zsh"
    "/usr/share/oh-my-zsh"
)
for dir in "${DIRS[@]}"; do
    if [ -f "$dir/oh-my-zsh.sh" ]; then
        export ZSH="$dir"
        break
    fi
done
export ZSH_CUSTOM="${DIRS[1]}/custom"
